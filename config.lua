Config = {}

Config.Debug = true
Config.DefaultImage = 'info.png'
Config.DefaultHeader = 'Infó'
Config.DefaultTime = 8

Config.Colors = {
    -- you can add your presets if you want.
    -- the default will always be the default if use did not give the last param in the event!
    ['error'] = 'rgba(255,0,0,0.35)',
    ['success'] = 'rgba(0,255,0,0.35)',
    ['blue'] = 'rgba(0,0,255,0.35)',
    ['default'] = 'rgba(0, 0, 0, 0.55)'
}
